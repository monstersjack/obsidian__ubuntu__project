---
标题: 未命名 1
创建时间: 2026-03-31
修改时间: 2026-07-06
---

这是一段基于 **Linux SocketCAN（can-utils 工具集）** 的电机初始化 Shell 脚本，通过 CAN 总线向双轮差速底盘的左右轮电机发送配置指令，确保测试从静止状态开始，并完成使能、模式切换、反馈周期配置。
### 下面逐行拆解含义：

---

#### 整体背景


---

#### 逐行命令详解

##### 1. 全局停机，确保初始静止

```bash
cansend can0 032#0000000000000000
sleep 0.5
```

- `032`：帧 ID（十六进制 0x032），为**全局停机指令帧**。
- `0000000000000000`：8 字节全 0，表示所有连接在 CAN 总线上的电机立即停止输出，进入自由停机/刹车状态。


##### 2. 使能左右轮电机

```bash
cansend can0 105#0A0A000000000000
sleep 0.2
```

- `105`：帧 ID（0x105），为**电机参数配置帧**，用于下发使能、模式切换等指令。
- `0A0A000000000000`：
  - 第 1 字节 `0A`（十进制 10）：对应 **ID1 右轮电机**，指令码 `0x0A` = 电机使能（上电、解除封锁、允许输出）。
  - 第 2 字节 `0A`：对应 **ID2 左轮电机**，同样执行使能操作。
  - 后 6 字节 `00`：ID3~ID8 电机保持原状态，不做修改。
- `sleep 0.2`：等待 200ms，让电调完成硬件初始化，避免后续指令丢失。

##### 3. 设置为速度环控制模式

```bash
cansend can0 105#0202000000000000
sleep 0.2
```

- 复用 0x105 配置帧，指令码含义变更：
  - 第 1 字节 `02`：ID1 右轮切换为**速度闭环模式**。
  - 第 2 字节 `02`：ID2 左轮切换为**速度闭环模式**。
- 速度环模式下，电调内部 PID 会根据目标转速自动调节电流，保证转速稳定，是轮式小车最常用的控制模式。
- `sleep 0.2`：等待模式切换生效。

##### 4. 设置主动反馈周期

```bash
cansend can0 106#0A0A000000000000
sleep 0.5
```

- `106`：帧 ID（0x106），为**反馈周期配置帧**。
- `0A0A000000000000`：
  - 第 1 字节 `0A`（十进制 10）：ID1 右轮的主动状态反馈周期设为 **10ms**，即电调每 10ms 主动向 CAN 总线上报一次实时转速、电流、编码器位置等状态。
  - 第 2 字节 `0A`：ID2 左轮同样设为 10ms 反馈周期。
- 10ms（100Hz）是移动机器人控制的常用配置，足够支撑里程计解算和速度闭环控制。
- `sleep 0.5`：等待配置生效，确保反馈数据稳定输出。

---

#### 补充说明
这套协议符合大疆 RoboMaster M3508/M2006 电机（C610/C620 电调）的简化控制逻辑，是差速底盘、AGV 小车最典型的初始化流程，核心目的是**安全上电、明确控制模式、固定反馈频率**，为后续的速度控制和里程计采集提供稳定的初始状态。