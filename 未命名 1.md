---
标题: 未命名 1
创建时间: 2026-03-31
修改时间: 2026-07-20
---

以下“外测编码器”按“外侧、靠近输出端的输出轴编码器”理解。针对 **DM‑J4310‑2EC**，官方资料可以确定：它采用两套单圈磁编码器，分别测量电机转子和减速器输出轴。

####  二、两套编码器汇总

| 编码器     | 安装位置                         | 测量对象        | 主要功能                                   | 工作原理                                | 故障影响                               |
| ------- | ---------------------------- | ----------- | -------------------------------------- | ----------------------------------- | ---------------------------------- |
| 电机侧编码器  | 电机转子轴端附近，传感器固定在壳体或驱动板一侧      | 高速电机转子      | 提供FOC换相角度、转速和转子位置；保证电机平稳输出转矩           | 轴端磁铁随转子旋转，固定磁传感器测量磁场方向并计算0～360°绝对角度 | 可能出现抖动、啸叫、无法启动、转矩异常或电机侧编码器校准异常     |
| 输出轴编码器  | 减速器输出端、输出法兰或输出轴附近，通常可以做成独立小板 | 减速器之后的真实输出轴 | 获得真实关节角度；上电后直接知道输出轴单圈绝对位置；补偿减速器回差和传动误差 | 输出轴磁铁随输出法兰旋转，固定磁传感器直接测量输出轴0～360°角度  | FOC可能仍能工作，但输出位置错误、位置控制异常或出现输出轴校准错误 |
| 电机侧折算位置 | 不是第三个编码器，是软件计算量              | 电机编码器角度÷减速比 | 估算输出轴位置，用于与直接输出角度比较                    | 根据减速比把转子累计角度换算到输出端                  | 无法完全反映齿轮回差、弹性变形和实际输出误差             |

因此，“双编码器”不是两个传感器重复测同一根轴，而是分别测量减速器的两侧：

```text
电机转子
   │
   ├── 电机侧编码器：供FOC换相和转速控制
   │
   ▼
10:1减速器
   │
   ├── 回差、摩擦、弹性变形、装配误差
   │
   ▼
输出轴
   │
   └── 输出轴编码器：测量负载实际得到的角度
```

####  三、磁编码器的基本原理

DM‑J4310‑2EC官方资料明确写的是“磁编（单圈）”，不是电感编码器。

一套典型磁编码器包含：

1. 安装在旋转轴上的磁铁；
2. 固定不动的磁传感器芯片；
3. 信号处理和角度计算电路；
4. 向主控制器传输角度的数字接口。

轴上的磁铁通常采用径向或直径方向充磁。磁铁旋转时，传感器测得两个互相正交的磁场分量：

$$
B_x \approx B\cos\theta
$$


$$
B_y \approx B\sin\theta
$$


芯片利用反正切计算角度：

$$
\theta=\operatorname{atan2}(B_y,B_x)
$$


得到0～360°的单圈绝对角度，再转换成14位或16位数字量。

以V1.2的16位编码器为例：

$$
2^{16}=65536
$$


所以理论上每一圈可以划分为65,536个角度位置：

$$
\frac{360^\circ}{65536}\approx0.00549^\circ
$$


这只是数字分辨率。实际精度还受到以下因素影响：

- 磁铁与芯片是否同轴；
- 磁铁到芯片的轴向间隙；
- 磁铁是否发生倾斜；
- 周围钢铁零件造成的磁场畸变；
- 芯片非线性和温度漂移；
- PCB安装偏差；
- 校准数据是否正确。

####  四、为什么需要两个编码器

DM‑J4310‑2EC的减速比是10:1，即电机转子旋转约10圈，输出轴旋转1圈。

如果只有电机侧编码器，控制器可以通过：

$$
\theta_{\text{估算输出}}
=
\frac{\theta_{\text{转子累计}}}{10}
$$


估算输出轴位置，但这种计算看不到：

- 减速器齿轮回差；
- 齿轮啮合误差；
- 负载作用下的弹性扭转；
- 装配偏差；
- 传动机构打滑或损坏；
- 电机侧单圈编码器在掉电后的圈数不确定性。

增加输出轴编码器后，控制器可以直接获得：

$$
\theta_{\text{实际输出}}
$$


两种位置可以同时使用：

| 数据 | 含义 |
|---|---|
| 转子折算输出位置 | 电机编码器累计角度除以减速比 |
| 输出轴直接位置 | 输出轴编码器直接测得的真实角度 |
| 两者差值 | 包含齿轮回差、弹性变形、安装偏置和测量误差 |

两编码器差值可以辅助检测减速器状态、估计弹性扭转，但它不等同于真正的转矩传感器。实际转矩控制通常仍主要根据相电流和电机转矩常数计算。

####  五、“单圈绝对编码器”是什么意思

“单圈绝对”表示每个0～360°位置都有唯一编码，因此重新上电后，不需要先旋转寻找原点，就能知道轴当前处于这一圈的什么位置。

但是它本身通常不能区分：

- 当前是第1圈；
- 第2圈；
- 还是第100圈。

控制器可以在不断电时通过角度跨越0°/360°进行圈数累计；完全断电后，累计圈数是否保留要看控制器是否另外保存。官方强调的是“输出轴单圈绝对位置”，不能据此认为它是多圈绝对编码器。

####  六、内部四线小板是什么

如果你看到的小板：

- 位于减速器输出端；
- 靠近输出轴或输出法兰；
- 小板中心附近面对一块随输出轴转动的磁铁；
- 通过`5V、S1、S2、GND`四根线连接主驱动板；

那么它高度可能就是输出轴磁编码器板。

| 线名 | 可以确认或合理推断的作用 |
|---|---|
| 5V | 编码器板供电 |
| GND | 电源地和信号参考地 |
| S1 | 编码器信号线1 |
| S2 | 编码器信号线2 |

仅根据`S1/S2`不能确定具体协议。它们可能是时钟/数据、两路编码器信号或者厂家自定义接口。官方用户手册没有公开内部原理图，因此不能把它们直接认定为UART的TX/RX，也不能套用DM‑MC02开发板的P13/P14定义。

#####  为什么小板不与大驱动板合并

输出轴编码器必须靠近被测输出轴磁铁。大驱动板的位置主要由MOSFET散热、电源连接、电机相线和壳体空间决定，不一定正好位于输出轴磁铁上方。

独立小板能够：

- 保证传感器正对输出轴磁铁；
- 控制磁铁与芯片之间的间隙；
- 单独调整编码器同轴度；
- 绕开轴承、减速器和中空结构；
- 方便装配及更换；
- 让大驱动板继续放在适合散热和接线的位置。

因此这根线是内部编码器线束，不是将外设引到电机外部。

####  七、两套编码器的校准

#####  电机侧编码器校准

主要用于修正电机侧传感器安装偏差，并确定磁编码器机械角度与电机电角度之间的零点关系。

DM‑J4310‑2EC有14对极，转子机械转过一圈时，电角度变化14个周期：

$$
\theta_e=(14\theta_m+\theta_{\text{offset}})\bmod 2\pi
$$


如果电角度偏置错误，会导致：

- 电流无法正确产生转矩；
- 电机抖动或反转；
- 电流明显增大；
- 发热严重；
- 输出转矩下降。

#####  输出轴编码器校准

主要用于修正输出轴磁铁偏心、传感器安装误差和角度非线性。官方流程会让输出轴旋转一圈，并采集编码器原始数据波形，然后生成校准数据。

官方说明：

- 出厂时已经完成输出轴编码器校准；
- 校准数据保存在电机内部；
- 正常使用不需要重新校准；
- 更换驱动板、拆动编码器、移动磁铁、受到异常冲击或输出位置异常时，才考虑重新校准。

不要在机械装配不正确时反复校准。校准只能补偿一定范围的周期性误差，无法修复磁铁脱落、间隙严重错误、连接器接反或传感器损坏。

####  八、输出轴编码器常见异常

| 现象 | 可能原因 |
|---|---|
| 输出角度固定不变 | 小板未供电、线束断开、传感器损坏 |
| 角度突然跳变 | 接触不良、磁场过弱、通信干扰 |
| 每转一圈出现重复误差 | 磁铁偏心、传感器不同轴、校准数据异常 |
| 0°附近来回跳变 | 单圈角度跨越0°/360°时的软件处理问题 |
| 输出位置方向相反 | 磁铁方向、安装方向或软件方向参数错误 |
| 电机能转但位置控制错误 | 电机侧编码器正常、输出轴编码器异常 |
| 输出轴校准失败 | 输出磁编码器、磁铁、线束、机械安装或校准条件异常 |

拆装时尤其要避免：

- 带电插拔编码器线；
- 交换`S1/S2`或插反连接器；
- 移动轴端磁铁；
- 改变小板固定位置；
- 在磁铁附近遗留钢屑；
- 压迫或拉扯四芯线束；
- 校准前让输出端连接较大负载。

最终可以将DM‑J4310‑2EC理解为：**电机侧磁编码器负责“把电机本身控制好”，输出轴磁编码器负责“知道关节最终实际转到了哪里”。**

官方依据：[DM‑J4310‑2EC V1.2说明书](<E:/黎老师的文件/志聪买的电机/文件/电机文件DM-J4310-2EC-master/说明书/DM-J4310-2EC V1.2减速电机说明书V1.2定稿.pdf>)。