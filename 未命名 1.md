---
标题: 未命名 1
创建时间: 2026-03-31
修改时间: 2026-07-20
---

`PC14`和`PC13`不是电机的速度线、方向线或CAN数据线。它们只是两个“电源开关控制脚”，决定对应电机接口有没有12～24V电源。

```text
主电源12～24V
     │
     ├── 电源开关1 ← PC14控制 ── OUT1电源 + CAN1 ── 电机1
     │
     └── 电源开关2 ← PC13控制 ── OUT2电源 + CAN2 ── 电机2
```

## 1. PC14和PC13具体控制什么

| 控制脚    | 对应接口         | 设置为1                         | 设置为0             |
| ------ | ------------ | ---------------------------- | ---------------- |
| `PC14` | OUT1电机口、CAN1 | 把主电源送到OUT1，电源指示灯亮，电机1驱动器开始启动 | 切断OUT1的主电源，电机1断电 |
| `PC13` | OUT2电机口、CAN2 | 把主电源送到OUT2，电源指示灯亮，电机2驱动器开始启动 | 切断OUT2的主电源，电机2断电 |

`PC14=1`的意思是STM32让PC14输出高电平，大约3.3V。这个3.3V只负责控制板上的MOS电源开关，不负责给电机供电，也不承载电机电流。

实际电流路径是：

```text
12～24V主电源 → 板载大电流MOS开关 → OUT1/OUT2 → 电机
```

PC14/PC13只是控制MOS开关“开”还是“关”。

## 2. 这两个控制脚不负责什么

PC14和PC13不负责：

- 电机正转或反转
- 控制电机速度
- 控制电机位置
- 控制电机转矩
- 发送CAN命令
- 让电机进入闭环使能状态

它们只负责电机有没有电。

真正控制电机运动的是CAN命令：

```text
PC14/PC13：接通电源
CAN使能命令：让电机进入工作状态
CAN控制命令：指定位置、速度或转矩
```

因此，电机开始运动通常需要三个条件同时满足：

| 条件 | 作用 |
|---|---|
| 主电源已接入 | 控制板有12～24V来源 |
| `PC14=1`或`PC13=1` | 对应电机口真正获得电源 |
| 发送CAN使能及控制命令 | 电机进入闭环并执行动作 |

仅仅设置`PC14=1`，电机通常只会上电自检，不会自动旋转。

## 3. OUT1和OUT2电机接口

每个电机口都是XT30(2+2)，包含电源和CAN：

| 触点 | 功能 |
|---|---|
| 1 | VCC，12～24V电机电源 |
| 2 | GND |
| 3 | CAN_L |
| 4 | CAN_H |

对应关系：

| 电机接口 | 电源开关 | CAN通道 |
|---|---|---|
| OUT1 | `PC14` | CAN1 |
| OUT2 | `PC13` | CAN2 |

如果只有一个DM-J4310电机，建议先接OUT1：

```text
电机接OUT1
使用CAN1
PC14=1
PC13=0
```

这样第二路电机口保持关闭，更安全。

## 4. `5A`是什么意思

“每路持续5A”并不是板子固定输出5A，而是表示这一路电路允许长期承载的电流能力约为5A。

例如：

- 电机空载只需要0.5A，板子就输出0.5A。
- 电机负载增加需要3A，板子就通过3A。
- 电机堵转需要超过5A，可能导致接口、MOS管、线材发热甚至损坏。

它不是精确限流器。初次测试应使用带限流功能的电源，并添加合适的保险丝。

## 5. 其他电源功能详细解释

| 功能 | 控制/参数 | 详细作用 |
|---|---|---|
| 主电源输入 | `12～24V` | 给板载降压电路和两路电机输出提供能源；防反接用于降低接反正负极造成的损坏，缓启动用于减小刚插电时的冲击电流 |
| 电机输出1 | `PC14=1` | 打开第一路高边MOS开关，把主输入电压送到OUT1；同时使用CAN1控制该电机 |
| 电机输出2 | `PC13=1` | 打开第二路高边MOS开关，把主输入电压送到OUT2；同时使用CAN2控制该电机 |
| 外设5V | `PC15=1` | 启动独立的5V降压输出，给舵机PWM、UART7、UART10和扩展接口的5V引脚供电，总电流上限约2A |
| USB Type-C | USB 5V | 可以启动STM32、下载程序和给CAN收发器供电；没有12～24V主电源时，电机输出口仍然没有功率电源 |
| 电压检测 | `PC4/ADC1_CH4` | STM32读取经过分压后的主电源电压，软件可以显示电池电压、判断欠压或记录电压变化 |

### PC15和PC13/PC14的区别

| 控制脚 | 控制什么 |
|---|---|
| `PC14` | OUT1的12～24V电机电源 |
| `PC13` | OUT2的12～24V电机电源 |
| `PC15` | 外部传感器、舵机、串口设备所需的5V |

`PC15=0`时，STM32本身仍然可以工作，只是部分接口旁边的5V电源没有输出。

## 6. STM32程序中怎样控制

打开OUT1：

```c
HAL_GPIO_WritePin(GPIOC, GPIO_PIN_14, GPIO_PIN_SET);
```

关闭OUT1：

```c
HAL_GPIO_WritePin(GPIOC, GPIO_PIN_14, GPIO_PIN_RESET);
```

打开OUT2：

```c
HAL_GPIO_WritePin(GPIOC, GPIO_PIN_13, GPIO_PIN_SET);
```

关闭OUT2：

```c
HAL_GPIO_WritePin(GPIOC, GPIO_PIN_13, GPIO_PIN_RESET);
```

打开外设5V：

```c
HAL_GPIO_WritePin(GPIOC, GPIO_PIN_15, GPIO_PIN_SET);
```

## 7. 推荐的安全启动顺序

以一个电机接OUT1为例：

1. 程序启动时先设置`PC14=0`、`PC13=0`。
2. 初始化CAN1为经典CAN、1Mbps。
3. 确认目标位置、速度、转矩均为安全值。
4. 设置`PC14=1`，电机获得电源。
5. 等待电机完成上电自检。
6. 发送达妙协议的电机使能命令。
7. 从很小的位置变化或很低的速度开始测试。

停止时建议：

1. 先发送零速度或安全控制命令。
2. 发送电机失能命令。
3. 最后设置`PC14=0`切断电机电源。

不要把`PC14=0`当成日常停车命令频繁直接切断；正常停车应先通过CAN减速、失能，再切断电源。PC14/PC13也不是经过安全认证的急停装置。