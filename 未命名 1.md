---
标题: 未命名 1
创建时间: 2026-03-31
修改时间: 2026-07-20
---

行星减速器本身不是电机，也不产生动力。它接收电机转子的高速旋转，把它转换成输出端的低速、大转矩旋转。

以 DM-J4310 的 `10:1` 为例：

```text
电机转子转10圈
        ↓
减速器输出端转约1圈
```

####  一、主要作用

| 作用 | 简单理解 |
|---|---|
| 降低转速 | 电机转得很快，经过减速器后输出变慢 |
| 放大转矩 | 输出端推、拉、抬重物的能力增强 |
| 提高控制分辨率 | 电机转动10°，输出端只转约1°，更容易精细控制 |
| 承受负载 | 多个行星齿轮共同传力，可以分担较大的负载 |
| 保持同轴 | 输入轴和输出轴位于同一中心，结构紧凑 |

减速器不能创造能量，因此实际关系是：

```text
输出转速 ≈ 电机转速 ÷ 减速比
输出转矩 ≈ 电机转矩 × 减速比 × 传动效率
```

例如，假设减速比为 `10:1`、效率为90%：

```text
电机转速：1000 rpm
输出转速：1000 ÷ 10 = 100 rpm

电机转矩：0.4 N·m
输出转矩：0.4 × 10 × 0.9 ≈ 3.6 N·m
```

损失的部分主要变成齿轮摩擦产生的热量和噪声。

####  二、行星减速器有哪些零件

常见行星减速器包含四个主要部分：

| 零件 | 是否转动 | 作用 |
|---|---:|---|
| 太阳轮 | 转动 | 位于中心，通常连接电机转子，是动力输入端 |
| 行星轮 | 自转并公转 | 同时与太阳轮和齿圈啮合，传递动力 |
| 内齿圈 | 通常固定 | 固定在外壳内部，限制行星轮的运动轨迹 |
| 行星架 | 转动 | 安装所有行星轮，通常作为减速后的输出端 |

结构关系可以简化为：

```text
电机转子
   ↓
太阳轮高速旋转
   ↓
多个行星轮一边自转、一边绕太阳轮公转
   ↓
带动行星架缓慢旋转
   ↓
输出盘低速、大转矩旋转
```

之所以叫“行星”，是因为行星轮的运动类似行星：

- 行星轮绕自己的轴旋转，称为“自转”。
- 行星轮同时绕中心太阳轮运动，称为“公转”。

####  三、它为什么能够减速

常见工作方式是：

```text
太阳轮：输入
内齿圈：固定
行星架：输出
```

电机带动太阳轮高速旋转。行星轮不能直接自由离开，因为外侧还与固定的内齿圈啮合，所以它们只能沿着内齿圈慢慢“爬行”。

行星轮的公转速度比太阳轮慢，因此连接所有行星轮的行星架也缓慢转动，最终实现减速。

```text
太阳轮快速转动
     ↓
行星轮沿固定齿圈缓慢移动
     ↓
行星架缓慢转动
```

同时，太阳轮输入的力通过多个行星轮共同传递给行星架，所以输出端能够得到更大的转矩。

####  四、多个行星轮有什么作用

一般会使用3个或更多行星轮，而不是只使用一个。

主要原因是：

- 多个齿轮共同承担转矩。
- 受力分布更均匀。
- 中心太阳轮不容易被推向一侧。
- 能在较小体积内传递较大转矩。
- 输入轴和输出轴可以保持同轴。

不过，这并不表示行星轮数量越多一定越好。齿轮数量增加后，加工、装配和载荷均分会更加困难。

####  五、它和电机转子的关系

完整动力过程是：

```text
驱动板控制U、V、W三相电流
          ↓
定子绕组产生旋转磁场
          ↓
永磁电机转子旋转
          ↓
转子带动太阳轮
          ↓
行星轮和内齿圈产生减速作用
          ↓
行星架带动输出盘旋转
```

所以：

- 电机定子和转子负责产生动力。
- 行星减速器负责改变转速和转矩。
- 行星架不是“另一个电机转子”，它只是机械输出部件。
- 减速器里面没有三相绕组，不需要电流驱动。

####  六、10:1对控制有什么影响

假设减速比为10：

| 电机转子侧 | 输出端 |
|---:|---:|
| 转10圈 | 约转1圈 |
| 转速1000 rpm | 约100 rpm |
| 转动10° | 约转动1° |
| 转矩0.4 N·m | 理想约4 N·m，实际更低 |

控制器必须知道减速比，才能把电机侧编码器数据换算成输出端位置：

```text
输出位置 ≈ 电机转子位置 ÷ 10
输出速度 ≈ 电机转子速度 ÷ 10
```

DM-J4310 还有输出侧编码器，可以直接测量输出盘实际角度，用来补偿齿轮间隙和传动误差。

需要严谨说明：DM-J4310 官方参数表明确写了内置减速器为 `10:1`，但参数表没有直接注明其内部一定属于行星式。以上是按照常见的“太阳轮输入、齿圈固定、行星架输出”的行星减速结构解释。