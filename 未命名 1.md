---
标题: 未命名 1
创建时间: 2026-03-31
修改时间: 2026-07-19
---

这里必须先纠正一个容易误导的说法：

---

####  二、P：目标位置 Position

`P` 是目标位置，单位是 `rad`。

例如：

```text
实际位置 = 0.2 rad
目标位置 = 1.0 rad
```

电机会向正方向运动，直到接近 `1.0 rad`。

如果：

```text
实际位置 = 1.5 rad
目标位置 = 1.0 rad
```

电机会向负方向返回。

这里的位置通常是相对于保存零点的绝对位置，不是“从当前位置再移动多少”。

PVT 中的运动方向主要由位置误差自动决定：

```text
目标位置 > 实际位置 → 正方向
目标位置 < 实际位置 → 负方向
```

因此 PVT 的电流上限通常只需要填写正数，因为它表达的是“最大允许用力的幅值”，正负方向由控制器根据位置误差决定。

---

####  三、V：速度上限 Velocity Limit

PVT 中的 `V` 和 MIT 中的 Velocity 含义不同。

#####  MIT 中

```text
Velocity = 目标速度
```

它参加：

$$
K_d(v_{\rm des}-v)
$$


的计算。

#####  PVT 中

```text
Velocity = 最大运动速度
```

它不是要求电机始终保持这个速度，而是说：

> 电机向目标位置运动时，速度绝对值不能超过这个数值。

例如：

```text
目标位置 = 2 rad
速度上限 = 1 rad/s
```

电机会向 `2 rad` 运动，但内部规划速度不会超过约 `1 rad/s`。

当快接近目标位置时，它会主动减速，最后停在目标附近，不会一直保持 `1 rad/s`。

如果负载很重或者电流上限很小，实际速度可能只有：

```text
0.2 rad/s
```

即使设置了：

```text
速度上限 = 1 rad/s
```

也不矛盾，因为这是上限，不是保证值。

#####  协议编码

官方力位混控协议规定：

```text
V_des = 实际速度上限 × 100
```

使用无符号 16 位整数发送。

例如：

| 实际速度上限 | 协议整数 |
|---:|---:|
| `0.5 rad/s` | `50` |
| `1 rad/s` | `100` |
| `2 rad/s` | `200` |
| `10 rad/s` | `1000` |

官方协议允许整数 `0～10000`，对应 `0～100 rad/s`。这仍然只是协议范围，不能视为电机实际安全速度。

---

####  四、T：转矩/电流上限

PVT 标签中的 `T` 很容易被理解成“目标转矩”，但达妙协议实际发送的是：

```text
i_des：扭矩电流限定标幺值
```

它更接近：

```text
允许使用的最大电流占电机最大电流的比例
```

定义为：

$$
i_{\rm pu}=\frac{I_{\rm limit}}{I_{\rm max}}
$$


协议把这个比例乘以 `10000` 后发送。

| 电流上限比例 | 含义         |    协议整数 |
| -----: | ---------- | ------: |
|    `0` | 不允许主动输出电流  |     `0` |
| `0.05` | 最大电流的 5%   |   `500` |
| `0.10` | 最大电流的 10%  |  `1000` |
| `0.20` | 最大电流的 20%  |  `2000` |
| `0.50` | 最大电流的 50%  |  `5000` |
| `1.00` | 最大电流的 100% | `10000` |

假设某个型号驱动器定义：

```text
最大电流 Imax = 8 A
```

那么：

```text
i_des = 0.20
```

对应的电流限制大约是：

```text
8 × 0.20 = 1.6 A
```

但注意，不同型号和固件中的“最大电流基准”可能不是铭牌额定电流，也不一定等于说明书中的峰值电流，应以上电打印、寄存器和对应型号协议为准。

#####  它是上限，不是固定输出

设置：

```text
电流上限 = 20%
```

并不代表电机会一直输出 20% 电流。

如果电机只需要 5% 的电流就能运动，它就只输出约 5%；只有位置误差很大、负载很重或轴被挡住时，控制器才可能达到 20% 上限。

因此：

```text
实际输出电流 ≤ 设置的电流上限
```

而不是：

```text
实际输出电流 = 设置的电流上限
```

#####  它不是 MIT 的前馈转矩

两者差别很大：

| 参数                | 含义                    |
| ----------------- | --------------------- |
| MIT `t_ff=1 N·m`  | 直接额外增加约 `+1 N·m` 转矩命令 |
| PVT `i_des=20%`   | 只规定正负方向最大允许使用约 20% 电流 |
| MIT `t_ff=-1 N·m` | 可以直接命令负方向转矩           |
| PVT 电流限制          | 通常是正的幅值，方向由位置误差决定     |

---

例如：

```text
目标位置     = 1.0 rad
速度上限     = 2.0 rad/s
电流上限比例 = 0.20
```

编码为：

```text
位置：1.0 float → 00 00 80 3F
速度：2.0×100 = 200 → C8 00
电流：0.20×10000 = 2000 → D0 07
```

完整数据为：

```text
CAN ID：0x301
数据：00 00 80 3F C8 00 D0 07
```

这个命令的意思是：

> “移动到 1 rad，运动速度不要超过 2 rad/s，输出电流不要超过最大电流的 20%。”

达妙官方的力位混控说明、缩放方式和 `0x300+ID` 帧格式见 [DM-J4340P-2EC 官方中文说明书](https://damiao.enactic.ai/en/assets/files/dm-j4340p-2ec-v1.0-zh-8460bcc658b2f0e8830a63d4dffc18df.pdf)。

---

####  六、PVT 适合哪些场景

#####  1. 机械臂关节柔和定位

例如让关节移动到 `30°`，但限制运动速度和最大用力：

```text
P = 0.524 rad
V = 0.5 rad/s
T = 低电流上限
```

即使碰到障碍物，电机能够使用的电流也受到限制。

但这不是功能安全意义上的防夹，需要机械限位、急停和安全控制共同实现。

#####  2. 夹爪闭合

可以把目标位置设置在物体内部：

```text
目标位置 = 完全闭合位置
速度上限 = 较低
电流上限 = 允许的夹持力
```

夹爪碰到物体后无法继续到达目标，位置控制器会增加输出，但最终被电流上限限制，于是形成近似恒定的最大夹持力。

#####  3. 压紧和顶靠

例如电机推动机构压到工件：

- P 决定最终希望到哪里。
- V 限制接触速度。
- T 限制最大压紧力。

但电流与输出端实际接触力之间还受到减速器效率、摩擦、力臂和机构几何关系影响。要求精确接触力时仍需要力传感器。

#####  4. 柔和限位或软着陆

让机构靠近机械限位时降低：

```text
速度上限
电流上限
```

这样碰撞冲击会比普通位置模式小。

---

####  七、四种主要控制模式对比

| 模式 | 主要输入 | Velocity 的含义 | Torque 的含义 | 适合场景 |
|---|---|---|---|---|
| MIT | P、V、KP、KD、Tff | 目标速度 | 有正负方向的前馈转矩 | 阻抗控制、机器人轨迹、直接力矩控制 |
| 位置/位置速度 | 目标位置、速度 | 最大运动速度 | 一般不能在每帧中动态指定 | 普通点到点定位 |
| 速度 | 目标速度 | 真正的目标速度 | 由内部速度环产生 | 轮子、转台、连续旋转 |
| PVT/力位混控 | 目标位置、速度上限、电流上限 | 最大运动速度 | 最大允许电流/转矩幅值 | 夹爪、压紧、柔和定位 |

最容易混淆的区别是：

```text
MIT 的 V：希望达到的速度
位置模式的 V：移动速度上限
PVT 的 V：移动速度上限
MIT 的 T：额外施加的前馈转矩
PVT 的 T：允许使用的最大转矩电流
```

---

####  八、位置模式详细说明

位置模式也常被称为：

```text
位置—速度模式
Pos-Vel Mode
梯形位置模式
```

输入通常是：

```text
目标位置 p_des
最大速度 v_des
```

驱动器内部有：

```text
位置环 → 速度环 → 电流环
```

软件只需要告诉它：

> “去哪里，以多快的最高速度过去。”

内部会根据配置的加速度和减速度生成梯形速度曲线：

```text
加速 → 匀速 → 减速 → 停在目标位置
```

CAN ID 通常是：

```text
0x100 + 电机ID
```

例如电机 ID 为 `1`：

```text
位置模式控制帧 ID = 0x101
```

数据为两个 32 位浮点数：

```text
D0～D3：目标位置
D4～D7：最高速度
```

位置模式的优点：

- 使用简单。
- 比 MIT 更容易进行普通定位。
- 内部自动处理加减速。
- 不需要每帧发送 KP、KD。

缺点：

- 无法像 MIT 一样每帧改变刚度、阻尼和前馈转矩。
- 普通协议帧不能动态给出一个明确的力矩上限。
- 遇到障碍物时，可能一直尝试追踪目标，直到触发内部电流或过载保护。

---

####  九、速度模式详细说明

速度模式的输入只有：

```text
目标速度 v_des
```

CAN ID 通常是：

```text
0x200 + 电机ID
```

例如电机 ID 为 `1`：

```text
速度模式控制帧 ID = 0x201
```

数据通常只使用前 4 字节：

```text
D0～D3：32位 float 目标速度
```

例如：

```text
v_des = 5 rad/s
```

就是让电机持续以约：

```text
5 × 60 ÷ 2π ≈ 47.7 rpm
```

运行。

在速度模式中：

- 正速度：正方向连续旋转。
- 负速度：反方向连续旋转。
- 零速度：速度环尝试让电机停住。

“目标速度为零”并不等同于失能。速度环仍可能输出转矩来抵抗外力，试图保持零速。

停止速度模式时比较稳妥的顺序通常是：

```text
先发送目标速度 0
等待电机停止
停止周期发送
再执行失能
```

---

####  十、MIT 模式详细定位

MIT 是四种模式中最灵活、也最需要上位机负责控制的一种：

$$
\tau_{\rm cmd}
=K_p(p_{\rm des}-p)
+K_d(v_{\rm des}-v)
+t_{\rm ff}
$$


它可以组合成：

| 设置方式 | 实现效果 |
|---|---|
| `Kp>0、Kd>0、v=0、tff=0` | 位置保持 |
| `Kp=0、Kd>0、v≠0` | 速度控制 |
| `Kp=0、Kd=0、tff≠0` | 直接转矩控制 |
| `Kp较小、Kd适当` | 柔顺位置/阻抗控制 |
| P、V、Tff 随轨迹不断更新 | 机器人动态轨迹控制 |

MIT 中的上位机通常要以稳定周期发送命令，例如 `1 ms` 或几毫秒一次。通信中断后是否自动失能，取决于超时保护参数。

---

####  十一、“周期位置、周期速度、周期扭矩”是什么

这些通常不是电机内部新增的三种控制模式，而是调试软件提供的自动测试信号发生器。

#####  周期位置

调试软件周期性改变目标位置，例如：

```text
+1 rad → -1 rad → +1 rad → -1 rad
```

用途：

- 检查往复定位。
- 观察过冲和振荡。
- 测试机械间隙。
- 绘制位置跟踪曲线。

风险：

- 会持续往复运动。
- 行程设置过大可能撞机械限位。
- KP 很大时换向冲击明显。

#####  周期速度

调试软件周期性改变目标速度，例如：

```text
+5 rad/s → -5 rad/s → +5 rad/s
```

用途：

- 测试正反转。
- 查看加减速响应。
- 检查速度环稳定性。

风险：

- 正负速度切换会产生较大制动电流。
- 快速减速时可能产生母线回生电压，导致超压。
- 机械惯量大时不适合高频换向。

#####  周期扭矩

通常在 MIT 模式下周期性改变 `t_ff`：

```text
+T → -T → +T
```

用途：

- 测试转矩响应。
- 检查电流环。
- 测试摩擦、间隙或负载动态特性。

这是三种周期测试中风险较高的一种，因为纯转矩命令没有固定位置目标。空载时即使转矩不大，电机也可能持续加速。

具体是正弦波、方波还是其他波形，以及幅值和周期怎样设置，要看调试助手版本；“周期”标签本身只能说明软件会自动重复改变命令。

---

####  十二、模式和状态不是一回事

“模式”回答的是：

```text
电机使用什么控制算法？
```

例如：

```text
MIT、位置、速度、PVT
```

“状态”回答的是：

```text
驱动器现在是否允许输出？有没有故障？
```

例如：

```text
失能、使能、超压、过流、过温
```

同一个 PVT 模式可以处于：

```text
PVT + 失能
PVT + 使能
PVT + 故障
```

模式没有改变，但运行状态不同。

---

####  十三、反馈状态码 ERR

官方反馈帧中的 `ERR` 状态通常为：

| ERR | 状态 | 详细含义 |
|---:|---|---|
| `0` | 失能 | 驱动器不进行正常闭环力矩输出 |
| `1` | 使能 | 驱动器处于正常闭环运行状态 |
| `8` | 超压 | 电源过高，或快速减速导致回生电压升高 |
| `9` | 欠压 | 电源电压低，或带载时电源明显跌落 |
| `A` | 过电流 | 命令过猛、KP过大、堵转、短路或负载冲击 |
| `B` | MOS过温 | 驱动功率管温度过高 |
| `C` | 电机过温 | 绕组温度过高 |
| `D` | 通讯丢失 | 在超时周期内没有收到有效 CAN 命令 |
| `E` | 过载 | 长时间大负载、堵转或持续高转矩 |

官方定义见 [达妙力位混控和反馈状态说明](https://damiao.enactic.ai/en/assets/files/dm-j4340p-2ec-v1.0-zh-8460bcc658b2f0e8830a63d4dffc18df.pdf)。

#####  超压 ERR=8

常见原因：

- 电源本身电压过高。
- 电机高速急停，机械能回灌到电源母线。
- 电源不能吸收回生能量。
- 周期速度正反转过快。

#####  欠压 ERR=9

常见原因：

- 电源电压设置太低。
- 电源功率不足。
- 导线过细或接触不良。
- 大转矩启动时电压瞬间下降。

#####  过流 ERR=A

常见原因：

- MIT 的 KP 突然设得很大。
- 目标位置与实际位置差太大。
- 转矩命令过大。
- 轴被机械结构卡死。
- 正反向切换过快。

#####  通讯丢失 ERR=D

常见原因：

- 点击“停止”后不再发送周期帧。
- CAN ID 或 Master ID 错误。
- CAN 波特率不一致。
- CAN_H/CAN_L 接错或终端电阻不合适。
- USB-CAN 或程序停止工作。

如果启用了通讯超时保护，停止发送命令后电机自动失能通常是正常保护行为。

---

####  十四、界面上的其他反馈状态

#####  实际位置 Position

来自编码器的实际位置，不是右侧输入的目标位置。

例如截图顶部：

```text
Position = 1.841954 rad
```

表示电机当前约位于：

```text
105.5°
```

#####  实际速度 Velocity

这是测量或估算的实际速度。接近零时因为量化可能显示：

```text
-0.007326 rad/s
```

这不一定代表电机真的在持续反转，可能只是编码刻度和测量噪声。

#####  实际转矩 Torque

通常由电机电流和转矩系数估算，不是独立的输出轴扭矩传感器测量值。因此减速器摩擦、效率和标定误差会影响它与真实外部负载转矩的对应关系。

#####  电机温度

通常指电机线圈或转子侧温度，用于判断绕组是否过热。

#####  驱动温度

通常指 MOSFET/功率板温度。驱动温度和电机温度可能相差很大，任何一个过高都可能触发保护。

#####  电机已使能

这是反馈帧确认的运行状态，比“刚刚点击了使能按钮”更可靠。点击使能但没有看到该反馈，说明命令可能没有被接收、模式不匹配或已经存在故障。

---

####  十五、“停止发送”和“电机失能”不同

这是非常重要的区别。

#####  停止

通常只是让调试软件停止定时发送控制帧：

```text
电脑不再发命令
```

此时电机是否马上失能取决于通讯超时设置。

#####  失能

明确通知驱动器退出闭环输出：

```text
电机停止主动输出力矩
```

负载可能下落。

#####  发送零命令

也不等于失能：

- MIT 中 `Kp=Kd=tff=0`：接近零转矩。
- 速度模式中 `v=0`：主动保持零速度。
- 位置模式中 `p=0`：主动转向保存零点。
- PVT 中 `p=0`：仍然可能主动向零点运动。

所以不能笼统地认为“全部输入零就是停车”。

---

####  十六、你的 DM-J4310 是否支持 PVT

这一点必须特别注意：

你截图里的调试助手是通用软件，显示 PVT 标签不代表连接的每一种电机都支持 PVT。

较早的 [DM-J4310-2EC V1.1 官方说明书](https://damiao.enactic.ai/assets/files/dm-j4310-2ec-v1.1-zh-5d089e0b013d8e2fc203c5b670008a66.pdf)只明确列出：

```text
MIT模式
位置速度模式
速度模式
```

没有列出力位混控/PVT。较新的型号或新固件才可能支持模式 `4`。

应当检查：

- 电机准确型号和硬件版本。
- 固件版本 `FW REV`。
- 参数页中的 `Control Mode` 是否可以设置为 `PVT/力位混控/模式4`。
- 对应型号协议是否定义了 `0x300+ID`。
- 写入模式后，串口上电信息是否确认 `Control Mode=PVT`。

仅仅点击调试界面中的 PVT 标签，通常只是让软件按 PVT 格式生成 CAN 帧，不一定会自动把电机内部模式切换成 PVT。

切换到位置或 PVT 前，最好先把目标位置设成当前实际位置，并在零速、失能状态下切换。否则目标为零而实际位置不为零时，使能后可能突然运动。