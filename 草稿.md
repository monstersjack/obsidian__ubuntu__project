---
标题: 草稿
创建时间: 2026-05-31
修改时间: 2026-07-19
---
你可以先用一句话记住：

- MIT：我每一刻都告诉电机“往哪、转多快、要多硬、额外用多大力”。
- 位置速度：告诉电机“去哪个位置，路上最多多快”。
- 速度：告诉电机“按这个速度一直转”。
- PVT/力位混控：告诉电机“去这个位置，但最多这么快、最多出这么大力”。

下面基于DM-J4310-2EC V1.2协议整理。最重要的修正是：PVT中的第三个参数不是目标转矩，而是最大允许电流。

## 1. 四种模式的控制含义

| 模式 | 通俗理解 | 每帧主要输入 | Position含义 | Velocity准确含义 | Torque/Current含义 |
|---|---|---|---|---|---|
| MIT | 上位机直接调节电机的“位置、速度、软硬和推力” | `P、V、KP、KD、Tff` | 目标位置 | 速度参考值，通过`KD`参与计算；不是限速值 | `Tff`是有正负方向的前馈转矩，可以主动推/拉 |
| 位置速度 | 给目的地和速度上限，电机自己规划过去 | `p_des、v_des` | 最终要到达的位置 | 运动过程允许的最大绝对速度，不是必须保持的速度 | 由内部位置环、速度环自动产生，不能每帧直接指定 |
| 速度 | 类似汽车定速巡航 | `v_des` | 不使用 | 真正需要维持的目标速度，正负号决定方向 | 内部速度环根据负载自动产生所需转矩 |
| PVT/力位混控 | 去目标位置，同时限制速度和最大用力 | `p_des、v_des、i_des` | 最终要到达的位置 | 最大允许速度，必须是正的限幅值 | `i_des`是最大相电流比例，只限制“最多用多大力”，不是目标转矩 |

MIT的基本关系可以理解为：

```text
输出转矩 ≈
KP ×（目标位置 - 实际位置）
+ KD ×（目标速度 - 实际速度）
+ 前馈转矩Tff
```

所以MIT里的速度只有在`KD>0`时才会产生作用；它也不是独立的速度闭环。

## 2. 电机实际会怎样运动

| 模式 | 启动后的行为 | 到位后会怎样 | 遇到阻挡会怎样 | 控制感觉 |
|---|---|---|---|---|
| MIT | 根据P、V、KP、KD、Tff立即计算输出 | `KP>0`时会保持目标位置；KP越大越“硬” | 继续按设定刚度和前馈力矩出力 | 可硬、可软，反应最快，但参数最复杂 |
| 位置速度 | 按设定加速度和限速运动到目标位置 | 内部位置环保持目标位置 | 位置误差一直存在时可能持续增加输出 | 平滑、稳定，适合普通定位 |
| 速度 | 不断调整输出，使速度接近设定值 | 不存在“到位”；会持续旋转 | 负载增大时自动增加转矩，直至达到电流限制 | 类似定速巡航 |
| PVT/力位混控 | 向目标位置运动，但速度和电流均受限制 | 尝试保持位置，但不会超过电流上限 | 达到电流上限后可能停在障碍物处，不再猛烈增加输出 | 比普通位置模式更柔和、更适合接触物体 |

## 3. 优点、缺点和适用场景

| 模式 | 最适合的场景 | 主要优点 | 不适合的场景 | 主要风险 |
|---|---|---|---|---|
| MIT | 机器人关节、机械臂轨迹、足式机器人、阻抗控制、弹簧感、直接转矩实验 | 上位机控制自由度最高，可以实时改变刚度、阻尼和转矩 | 初学者简单定位、只想转到一个角度 | KP过大会猛转，KD过小会振荡；零点错误时可能突然转动 |
| 位置速度 | 点到点定位、舵机式运动、机械臂缓慢移动、云台转角 | 参数少、运动平滑、内部自动完成位置闭环 | 精确力控制、碰撞柔顺、直接转矩控制 | 目标位置或零点错误会朝错误方向运动；遇到阻挡仍可能持续出力 |
| 速度 | 轮子、滚筒、输送带、转盘、风扇、泵、连续旋转机构 | 直接给速度即可，最适合连续旋转 | 精确停在某个角度、保持固定位置 | 给错正负方向会一直反向转；失控时可能持续加速 |
| PVT/力位混控 | 夹爪、压紧机构、柔和归位、推靠限位、接触物体 | 能同时限制速度和最大电流，比普通位置控制更不容易猛烈碰撞 | 精确恒力控制、直接输出正负转矩、快速机器人阻抗控制 | 电流百分比不等于精确夹持力；设太低到不了位置，设太高仍可能夹坏物体 |

## 4. 按实际任务选择

| 你的任务 | 推荐模式 | 原因 |
|---|---|---|
| 第一次测试电机能否转动 | 位置速度模式 | 设一个很小的位置变化和很低的速度，比较容易控制 |
| 让电机从当前位置转30° | 位置速度模式 | 只需给目标位置和限速 |
| 让轮子保持固定转速 | 速度模式 | 真正的速度闭环 |
| 让夹爪夹住物体但不要太用力 | PVT/力位混控 | 可以限制最大电流 |
| 让机械臂关节产生“弹簧感” | MIT | KP控制刚度，KD控制阻尼 |
| 让关节既跟踪轨迹又能柔顺碰撞 | MIT | 可以高速更新P、V、KP、KD和Tff |
| 直接输出正向或反向转矩 | MIT | `KP=0、KD=0`，使用带符号的`Tff` |
| 需要精确10N夹持力 | 外部力传感器＋闭环 | PVT电流限制只能近似限制力，不能保证精确牛顿值 |

## 5. PVT中的电流到底是什么意思

PVT控制帧中的`i_des`是标幺值：

```text
i_des = 0.20
```

表示最多允许使用最大相电流的20%。它具有以下特点：

- 范围是`0～1.0`，界面通常显示成`0～100%`。
- 它没有正负方向。
- 电机用力方向由“目标位置－实际位置”的方向决定。
- 它限制的是电流，不是直接限制输出轴的N·m。
- 实际输出力还会受到转矩常数、减速器、摩擦和机械结构影响。

因此PVT更准确的名字是：

```text
位置控制 + 速度上限 + 电流/力矩上限
```

而不是“位置、速度、转矩三个目标同时跟踪”。

## 6. 四种模式的CAN帧区别

假设电机CAN ID为`1`：

| 模式 | 发送CAN ID | 数据内容 |
|---|---:|---|
| MIT | `0x001` | P、V、KP、KD、Tff，共8字节压缩数据 |
| 位置速度 | `0x101` | p_des浮点数＋v_des浮点数，共8字节 |
| 速度 | `0x201` | v_des浮点数，共4字节 |
| PVT/力位混控 | `0x301` | p_des浮点数＋速度上限uint16＋电流上限uint16 |

不同模式必须发送对应的CAN ID和数据格式。

## 7. 初学者使用顺序

建议按照下面的顺序学习：

1. 位置速度模式：先学会低速转到一个小角度。
2. 速度模式：学会控制正转、反转和停止。
3. PVT：在夹爪或接触测试中学习限制最大电流。
4. MIT：最后学习KP、KD和Tff。

切换到位置相关模式前，应让电机接近零速，并把第一次目标位置设置为当前实际位置，防止使能后突然跳向旧零点。MIT位置控制时不要让`KD=0`；MIT转矩测试不能空载直接给较大`Tff`，因为即使很小的恒定转矩也可能让空载电机不断加速。

以上定义来自最新的 [DM-J4310-2EC V1.2说明书](<E:/黎老师的文件/志聪买的电机/文件/电机文件DM-J4310-2EC-master/说明书/DM-J4310-2EC V1.2减速电机说明书V1.2定稿.pdf>) 第10～16页和第35～41页。