---
标题: 草稿
创建时间: 2026-05-31
修改时间: 2026-07-19
---



####  二、Position：目标位置

单位为 `rad`，即弧度：

- `0 rad = 0°`
- `1 rad ≈ 57.3°`
- `π rad ≈ 3.1416 rad = 180°`
- `2π rad ≈ 6.2832 rad = 360°`

这里的 Position 通常是相对于电机当前保存零点的绝对目标位置，不是“从当前位置再转这么多”。

例如：

- 保存零点之后，当前位置为 `0 rad`。
- 输入 `1 rad`，目标位置就是零点正方向约 `57.3°`。
- 输入 `-1 rad`，就是零点反方向约 `57.3°`。
- 输入 `3.1416 rad`，就是相对于零点约 `180°`。

但是，Position 本身不会直接产生转矩，必须乘上 `Kp`：

$$
\tau_p=K_p(p_{\rm des}-p)
$$


如果：

```text
目标位置 = 1.0 rad
实际位置 = 0.8 rad
KP = 10 N·m/rad
```

那么：

```text
位置误差 = 1.0 - 0.8 = 0.2 rad
位置项转矩 = 10 × 0.2 = 2 N·m
```

电机会输出正方向约 `2 N·m`，把轴拉向目标位置。

如果电机超过目标位置：

```text
目标位置 = 1.0 rad
实际位置 = 1.1 rad
KP = 10
```

那么：

```text
位置误差 = -0.1 rad
位置项转矩 = -1 N·m
```

转矩方向自动反过来，把电机拉回去。

最重要的是：

> 如果 `KP=0`，无论 Position 填多少，位置项转矩都是零，Position 不起作用。

####  四、Velocity：目标速度

单位是 `rad/s`，即每秒转过多少弧度：

$$
{\rm rpm}={\rm rad/s}\times\frac{60}{2\pi}
$$


常用换算：

| rad/s | 大约 rpm |
|---:|---:|
| 1 | 9.55 |
| 5 | 47.7 |
| 10 | 95.5 |
| 20 | 191 |
| 30 | 286.5 |

但在 MIT 模式中，Velocity 不是“最大速度限制”，而是速度控制项的目标值：

$$
\tau_v=K_d(v_{\rm des}-v)
$$


如果：

```text
目标速度 = 5 rad/s
实际速度 = 3 rad/s
KD = 0.5
```

那么：

```text
速度误差 = 5 - 3 = 2 rad/s
速度项转矩 = 0.5 × 2 = 1 N·m
```

电机得到正方向转矩，继续加速。

当实际速度超过目标速度：

```text
目标速度 = 5 rad/s
实际速度 = 6 rad/s
KD = 0.5
```

则：

```text
速度项转矩 = 0.5 × (5 - 6)
           = -0.5 N·m
```

电机会产生反方向转矩，帮助减速。

最重要的是：

> 如果 `KD=0`，Velocity 填多少都不起作用。

---

####  五、KD：速度阻尼或速度增益

`Kd` 决定“每相差 1 rad/s，要产生多少转矩”。

它的单位可以理解为：

```text
N·m·s/rad
```

当目标速度设为零时：

$$
\tau_v=K_d(0-v)=-K_dv
$$


这说明它总是产生与实际运动方向相反的转矩，因此像减震器一样抑制振荡。

例如：

```text
目标速度 = 0
实际速度 = 2 rad/s
KD = 0.5
```

那么：

```text
速度项转矩 = 0.5 × (0 - 2)
           = -1 N·m
```

这相当于给电机施加 `1 N·m` 的反向制动力。

所以在位置控制中：

- `KP` 把电机拉向目标位置。
- `KD` 防止电机冲过头、来回摆动。

如果只有 KP、没有 KD，就像“弹簧没有减震器”，电机可能在目标位置两边不停振荡。官方因此明确警告：MIT 位置控制时不要让 `Kd=0`。[DM-J4310 MIT 模式说明](https://damiao.enactic.ai/en/products/hardware/dm-j4310-2ec-v1.1/)

不过 KD 也不是越大越好：

- 太小：压不住振荡。
- 太大：反应迟钝，运动阻力很强。
- 很大时还可能放大速度测量噪声，导致转矩抖动。

---

####  六、Torque：前馈转矩

这个 Torque 是：

$$
t_{\rm ff}
$$


也就是直接叠加到最终命令上的前馈转矩：

$$
\tau_{\rm cmd}=\tau_p+\tau_v+t_{\rm ff}
$$


它不是“最大转矩限制”。

例如：

```text
位置项 = 2 N·m
速度项 = -0.5 N·m
前馈转矩 = 0.8 N·m
```

最终命令为：

```text
2 - 0.5 + 0.8 = 2.3 N·m
```

常见用途包括：

- 直接进行转矩控制。
- 补偿机械摩擦。
- 补偿机械臂重力。
- 在位置控制之外额外提供推力。

如果：

```text
KP = 0
KD = 0
Torque = 1 N·m
```

那么理论上：

```text
最终命令转矩 = 1 N·m
```

这就是纯转矩控制。电机不会主动保持某个位置或速度，负载允许时会不断加速，因此需要特别谨慎。

---

####  七、五个参数怎样组合

#####  1. 保持位置

```text
Position = 需要保持的位置
Velocity = 0
KP > 0
KD > 0
Torque = 0
```

含义是：

- KP 把电机拉回目标位置。
- KD 抑制运动和振荡。
- 最终像一个带减震器的弹簧。

#####  2. 纯速度控制

```text
Position = 任意
Velocity = 目标速度
KP = 0
KD > 0
Torque = 0
```

因为 `KP=0`，Position 被忽略：

$$
\tau_{\rm cmd}=K_d(v_{\rm des}-v)
$$


#####  3. 纯转矩控制

```text
KP = 0
KD = 0
Torque = 目标转矩
```

Position 和 Velocity 都被忽略：

$$
\tau_{\rm cmd}=t_{\rm ff}
$$


#####  4. 轨迹控制

控制器每隔例如 `1 ms` 更新一次：

```text
Position = 当前轨迹目标位置
Velocity = 当前轨迹目标速度
KP、KD = 需要的刚度和阻尼
Torque = 重力或动力学补偿
```

这种情况下，目标位置和目标速度会随时间一起变化，而不是始终固定。

---

####  八、结合你截图中的实际数值

截图顶部反馈显示：

```text
实际位置 p ≈ 1.841954 rad ≈ 105.5°
实际速度 v ≈ -0.007326 rad/s
```

右侧发送参数是：

```text
目标位置 = 0
目标速度 = 0
KP = 0
KD = 0
Torque = 0
```

因此，虽然目标位置和实际位置相差约 `-1.842 rad`，但因为：

```text
KP = 0
```

位置项仍然是：

```text
0 × (0 - 1.841954) = 0 N·m
```

速度项和前馈项也接近零。因此当前控制帧基本是在命令“零转矩”，不会主动把电机拉到零点。

但假设你突然把 `KP` 改成 `5`：

```text
位置项 = 5 × (0 - 1.841954)
       ≈ -9.21 N·m
```

电机就可能突然向负方向猛烈转动。这正是“目标位置为零时，不要直接给大 KP”的原因。

更稳妥的逻辑是：准备保持当前位置时，应先让目标位置等于当前反馈位置，即大约 `1.842 rad`，这样初始位置误差接近零。

---

####  九、MIN/MAX 范围到底代表什么

| 参数 | 编码范围 | 含义 |
|---|---:|---|
| Position | `-12.5～12.5 rad` | CAN 能表示的位置区间 |
| Velocity | `-30～30 rad/s` | CAN 能表示的速度区间 |
| KP | `0～500` | Kp 的编码区间 |
| KD | `0～5` | Kd 的编码区间 |
| Torque | `-10～10 N·m` | CAN 能表示的前馈转矩区间 |

这些是通信协议的“尺子刻度”，不是电机实际性能。

以你说的 DM-J4310-2EC V1.1 为例，官方列出的硬件性能是：

- 额定转矩：`3 N·m`
- 峰值转矩：`7 N·m`
- 额定速度：`120 rpm`
- 空载最高速度：`200 rpm`

而 CAN 映射可以表示：

- 转矩到 `±10 N·m`
- 速度到约 `±286 rpm`

因此，发送 `10 N·m` 不代表这台电机真能安全输出 `10 N·m`；发送 `286 rpm` 也不代表它能达到这个速度。实际输出仍受电流、供电、负载、温度和保护参数限制。[DM-J4310 官方产品参数](https://damiao.enactic.ai/en/products/hardware/dm-j4310-2ec-v1.1/)

同样，位置 `±12.5 rad` 只是可以编码约 `±716°`。DM-J4310 官方标注的是单圈绝对位置编码器，因此不要仅根据 `±12.5 rad` 就认定它能提供可靠的多圈绝对位置；跨圈和位置回绕行为需要按具体固件验证。

####  十、为什么“零”编码成 `7FFF`

线性编码公式为：

$$
u=\frac{x-x_{\min}}{x_{\max}-x_{\min}}(2^n-1)
$$


解码则是：

$$
x=x_{\min}+
\frac{u}{2^n-1}(x_{\max}-x_{\min})
$$


协议采用无符号整数，所以：

- 最小值对应 `0x000...`
- 最大值对应 `0xFFF...`
- 零在整个编码范围的中间

你截图里的：

```text
7F FF 7F F0 00 00 07 FF
```

精确拆解是：

| 参数 | 编码 | 解码结果 |
|---|---:|---:|
| Position | `0x7FFF` | 约 `-0.000191 rad` |
| Velocity | `0x7FF` | 约 `-0.007326 rad/s` |
| KP | `0x000` | `0` |
| KD | `0x000` | `0` |
| Torque | `0x7FF` | 约 `-0.002442 N·m` |

之所以不是数学上的绝对零，是因为量化后的整数刻度恰好没有一个完全位于正负范围中点；调试软件通常把这种极小值当作零显示。

最后还要注意一个型号问题：你给出的网页链接实际指向 `DM-J4340-2EC`，而文字和中文 PDF 是 `DM-J4310-2EC V1.1`。两者的 MIT 帧结构相似，但额定转矩、峰值转矩和减速比差别很大，实际操作前应以电机铭牌型号为准。