---
标题: 未命名
创建时间: 2026-06-13
修改时间: 2026-06-27
---

### 第一阶段：实验准备与连接
1.  **供电准备**：将直流电源调至 **24V** 并关闭，连接好电路后再上电（参考2.2节）。
2.  **物理固定**：使用台钳固定电机定子，确保转子可自由旋转（参考2.3节）。
3.  **接线**：按图10连接红（正）、黑（负）、黄（CAN_H）、白（CAN_L）（参考2.5节）。
4.  **软件环境**：插入USB转CAN模块，打开“DM-AcTools”上位机，设置波特率为 **1000Kbps** 并连接（参考2.7节）。

---

### 第二阶段：电机初始化配置（配置指令）

在驱动电机前，需先设定电机的身份和工作模式。

**步骤 1：设置电机 ID（若有多台电机需区分）**
*   **指令标识符：`0x108`**
*   **操作**：设置当前电机为 ID 1。
*   **指令数据**：`01 00 00 00 00 00 00 00`

![[Pasted image 20260627171354.png]]
没有显示接受成功，这是不是没有修改成功？


**步骤 2：切换控制模式**
*   **指令标识符：`0x105`**
*   **选项 A（电流环模式）**：`01 01 01 01 01 01 01 01`
*   **选项 B（速度环模式）**：`02 02 02 02 02 02 02 02`（建议实验初次使用此模式）

![[Pasted image 20260627171439.png]]
![[Pasted image 20260627171523.png]]
切换速度模式和电流模型都没有问题


**步骤 3：设置反馈/报文上报方式**
*   **指令标识符：`0x106`**
*   **选项 A（主动上报 10ms）**：`0A 0A 0A 0A 0A 0A 0A 0A`（电机自动持续发送数据）
*   **选项 B（查询方式）**：`80 80 80 80 80 80 80 80`（仅在收到查询指令时回复）


![[Pasted image 20260627171633.png]]
主动上报，发送了3次，他才显示接收，然后，弹出的数据内容，

#### 根据，AI的意思是



---

### 第三阶段：电机驱动执行（驱动指令）

根据第二阶段设置的模式，发送对应的驱动指令。

**步骤 4：发送运动给定值（以 ID 1 为例）**
*   **指令标识符：`0x32`**（对应 ID 1~4 电机）
*   **操作 A：速度模式下运行 30RPM**
	*   指令数据：`01 2C 01 2C 01 2C 01 2C` (注：0x012C=300，即 30.0RPM 的 10 倍)
*   **操作 B：停止电机**
	*   指令数据：`00 00 00 00 00 00 00 00`
*   **操作 C：反转 30RPM**
	*   指令数据：`FE D4 00 00 00 00 00 00` (注：0xFED4 为 -300)

---

### 第四阶段：状态监控与查询（监控指令）

如果在步骤 3 中设置了“查询方式”，则需要手动发送查询指令获取数据。

**步骤 5：查询电机实时状态**
*   **指令标识符：`0x107`**
*   **操作**：查询电机 1 的速度（0x01）、位置（0x04）、故障码（0x05）。
*   **指令数据**：`01 01 04 05 A1 00 00 00`（A1 为自定义值，用于区分回帧）

---

### 实验总结（指令集速查表）

| 实验环节     | 功能              | 标识符 (HEX) | 典型指令示例 (DATA 域)           |
| :------- | :-------------- | :-------- | :------------------------ |
| **初始化**  | 设置 ID 1         | `0x108`   | `01 00 00 00 00 00 00 00` |
| **模式切换** | 切换至速度环          | `0x105`   | `02 02 02 02 02 02 02 02` |
| **反馈设置** | 设置 1ms 主动上报     | `0x106`   | `01 01 01 01 01 01 01 01` |
| **驱动控制** | 正转 30RPM (ID 1) | `0x32`    | `01 2C 00 00 00 00 00 00` |
| **驱动控制** | 电流环给定 2000      | `0x32`    | `07 D0 07 D0 07 D0 07 D0` |
| **数据查询** | 查询速度/位置/故障      | `0x107`   | `01 01 04 05 A1 00 00 00` |