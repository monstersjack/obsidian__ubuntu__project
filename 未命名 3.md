---
标题: 未命名 3
创建时间: 2026-05-15
修改时间: 2026-07-19
---

最简单的理解是：

> **KP 看“位置差多少”，KD 看“现在冲得有多快”。**

先把目标速度设为 `0`、前馈转矩设为 `0`，公式就简化成：

$$
\boxed{\tau=K_p(p_{\rm des}-p)-K_dv}
$$


也就是：

```text
最终转矩 = 拉回目标位置的转矩 + 阻止快速运动的转矩
```

#####   KP：像橡皮筋或弹簧

想象电机轴和目标位置之间连着一根虚拟弹簧：

- 电机离目标越远，弹簧拉力越大。
- 电机离目标越近，弹簧拉力越小。
- 电机正好在目标位置，弹簧不再用力。
- `KP` 越大，弹簧越硬。

计算公式：

$$
\tau_p=K_p(p_{\rm des}-p)
$$


假设：

```text
目标位置 = 1 rad
实际位置 = 0.8 rad
KP = 2 N·m/rad
```

则：

```text
位置误差 = 1 - 0.8 = 0.2 rad
KP产生的转矩 = 2 × 0.2 = 0.4 N·m
```

电机受到正方向 `0.4 N·m` 的转矩，向目标位置运动。

如果电机超过目标：

```text
目标位置 = 1 rad
实际位置 = 1.1 rad
KP = 2
```

则：

```text
位置误差 = 1 - 1.1 = -0.1 rad
KP产生的转矩 = 2 × (-0.1)
             = -0.2 N·m
```

负号表示反方向，电机会被重新拉回目标位置。

所以可以记成：

> **KP 负责“把电机拉到哪里”。**

---

#####   KD：像门上的缓冲器

只有弹簧还不够。

想象一扇装有弹簧的门。如果没有缓冲器，门可能：

```text
冲过目标 → 被拉回来 → 又冲过目标 → 来回振荡
```

KD 就像门上的液压缓冲器：

- 运动得越快，反向阻力越大。
- 运动得越慢，阻力越小。
- 完全不动时，阻力为零。
- `KD` 越大，缓冲作用越强。

当目标速度为零时：

$$
\tau_d=-K_dv
$$


假设电机正方向运动：

```text
实际速度 = +2 rad/s
KD = 0.5
```

那么：

```text
KD产生的转矩 = -0.5 × 2
             = -1 N·m
```

电机正方向运动，KD 产生负方向转矩，因此它在刹车。

如果电机向负方向运动：

```text
实际速度 = -2 rad/s
KD = 0.5
```

那么：

```text
KD产生的转矩 = -0.5 × (-2)
             = +1 N·m
```

电机向负方向运动，KD 产生正方向转矩，仍然是在刹车。

所以可以记成：

> **KD 负责“别冲得太快，别来回振荡”。**

---

#####   一个完整过程

假设：

```text
目标位置 p_des = 1 rad
目标速度 v_des = 0
KP = 2
KD = 0.5
```

######   1. 电机静止在 0 rad

```text
实际位置 = 0 rad
实际速度 = 0 rad/s
```

KP：

```text
2 × (1 - 0) = +2 N·m
```

KD：

```text
0.5 × (0 - 0) = 0
```

最终：

```text
+2 + 0 = +2 N·m
```

电机开始向目标运动。

######   2. 电机已经接近目标，但速度很快

```text
实际位置 = 0.8 rad
实际速度 = 2 rad/s
```

KP：

```text
2 × (1 - 0.8) = +0.4 N·m
```

KD：

```text
0.5 × (0 - 2) = -1 N·m
```

最终：

```text
+0.4 - 1 = -0.6 N·m
```

虽然还没到目标，但因为冲得太快，KD 已经开始刹车，防止冲过头。

######   3. 电机到达目标，但还在运动

```text
实际位置 = 1 rad
实际速度 = 0.5 rad/s
```

KP：

```text
2 × (1 - 1) = 0
```

KD：

```text
0.5 × (0 - 0.5) = -0.25 N·m
```

最终：

```text
-0.25 N·m
```

位置已经正确，所以 KP 不再用力；但电机还在动，所以 KD 继续刹车。

######   4. 电机停在目标位置

```text
实际位置 = 1 rad
实际速度 = 0
```

KP：

```text
2 × (1 - 1) = 0
```

KD：

```text
0.5 × (0 - 0) = 0
```

最终转矩为零。此时电机到达目标并停止。

如果有外力把电机推离目标，位置误差重新出现，KP 就会产生转矩把它拉回来。

#####   最关键的区别

| 情况 | KP 是否起作用 | KD 是否起作用 |
|---|---|---|
| 停着，但位置不对 | 起作用 | 不起作用 |
| 位置正确，但还在运动 | 不起作用 | 起作用 |
| 位置不对，而且正在运动 | 起作用 | 起作用 |
| 位置正确，而且已经停止 | 不起作用 | 不起作用 |

因此：

- **KP 不关心电机运动多快，只关心位置差。**
- **KD 不关心电机在哪里，只关心速度差。**

一句笔记版：

```text
KP：位置纠错——离目标越远，拉得越用力。
KD：速度阻尼——运动越快，反向刹车越强。
KP决定“硬不硬”，KD决定“稳不稳”。
```