---
标题: 未命名 2
创建时间: 2026-03-31
修改时间: 2026-07-20
---

这里说的“电机1、机2”不是两种不同型号的电机，也不是开发板自带两台电机。

它表示DM-MC02开发板可以同时连接两台外部CAN电机。

两路电机接口的功能基本相同，只是独立控制。

例如你有两台DM-J4310：

- 第一台DM-J4310接OUT1，程序设置`PC14=1`，通过CAN1控制。
- 第二台DM-J4310接OUT2，程序设置`PC13=1`，通过CAN2控制。

如果你现在只有一台DM-J4310，使用其中一路即可。建议：

```text
DM-J4310 → OUT1
PC14 = 1
PC13 = 0
使用CAN1
```

这里的“电机1”和“电机2”只是接口编号，与你设置的电机CAN ID也不是一回事：

| 名称       | 含义              |
| -------- | --------------- |
| OUT1/电机1 | 开发板第一路物理电机接口    |
| OUT2/电机2 | 开发板第二路物理电机接口    |
| CAN ID=1 | CAN总线上某台电机的通信地址 |

因此，OUT1不一定只能接CAN ID为1的电机，OUT2也不一定要接CAN ID为2的电机。CAN ID可以根据需要配置。

另外，每个OUT口都包含两部分：

- 12～24V和GND：给电机驱动器供电。
- CAN_H和CAN_L：向电机发送控制命令并接收反馈。

`PC14`和`PC13`只控制对应接口的电源，不决定电机型号，也不直接决定电机怎样转。