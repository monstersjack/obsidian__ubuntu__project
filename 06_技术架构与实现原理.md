---
标题: 06_技术架构与实现原理
创建时间: 2026-05-31
修改时间: 2026-07-03
---

### 一、串口通信的基本概念
串口通信（Serial Communication）是**以单个比特（bit）为单位，在一条（或一对差分）传输线上按顺序逐位传输数据**的通信方式，与“多位同时传输”的并行通信相对。

核心特点：
- 线路少、布线成本低，适合中长距离传输；
- 单位时间内的峰值传输速率低于同带宽的并行通信；
- 广泛用于板级芯片通信、设备调试打印、工业现场总线等场景。

### 二、串口通信的分类
串口通信可以从**同步机制**和**电气接口标准**两个维度划分，日常口语中所说的“串口”，通常特指基于UART的异步串行接口。

#### 1. 按同步机制划分
##### （1）异步串口（代表：UART）
没有独立的时钟线，收发双方依靠**约定的波特率 + 帧起始/停止位**实现时序同步。
- 优点：接线极简（仅需TX、RX、GND三根线）；
- 缺点：每帧数据需附带起始位、停止位等开销，有效数据占比略低。

##### （2）同步串口（代表：SPI、I2S、USART同步模式）
有专门的时钟线提供同步基准，数据在时钟节拍下传输，无需起始/停止位。
- 优点：传输效率高、支持更高速率；
- 缺点：多一根时钟线，多用于短距离板级通信。

> 注：SPI、I²C 也属于广义的串行通信，但嵌入式开发中默认说“串口”时，指代的都是UART体系的异步串口。

#### 2. 按电气电平/接口标准划分（基于UART协议）
这是最常用的分类方式——它们的底层通信协议均为UART异步帧格式，区别只在电平标准、传输距离和组网能力：

| 类型 | 电平规则 | 典型接线 | 工作模式 | 典型传输距离 | 主要应用场景 |
|------|----------|----------|----------|--------------|--------------|
| TTL-UART | 逻辑0：0V；逻辑1：3.3V / 5V | TX、RX、GND 三线 | 全双工 | 几米内（板级） | 单片机与外设模块通信、芯片级调试 |
| RS-232 | 负逻辑：逻辑1：-3~-15V；逻辑0：+3~+15V | DB9接口，常用TX、RX、GND | 全双工 | 约15米 | 老式电脑串口、短距工控设备 |
| RS-422 | 差分电平（A/B线电压差判定） | TX+、TX-、RX+、RX-、GND | 全双工 | 最长约1200米 | 工业长距离全双工通信 |
| RS-485 | 差分电平（A/B线电压差判定） | A、B、GND 两线为主 | 半双工（主流） | 最长约1200米 | 工业现场总线、Modbus组网、多设备串联 |

### 三、UART 详解
UART 全称 **Universal Asynchronous Receiver/Transmitter（通用异步收发器）**，是实现异步串口通信的硬件模块，几乎所有单片机、嵌入式SOC都内置该外设。

#### 1. 核心工作原理
- **发送端**：将CPU传来的并行数据，按照约定的帧格式转换成串行比特流，从TX引脚逐位发出；
- **接收端**：从RX引脚逐位采集串行比特流，还原成并行数据后交给CPU。

收发双方不需要额外时钟线，依靠**完全一致的波特率**和统一的帧结构来识别每一个数据位。

#### 2. UART 数据帧结构
一帧完整的UART数据由4部分组成，工业界最常用的配置是 **8N1**（8位数据、无校验、1位停止位）：
1. **起始位**：1位，始终为低电平，用来标志一帧数据的开始；
2. **数据位**：5~8位可选，承载实际传输的数据，传输顺序为**低位在前、高位在后**；
3. **校验位**：可选（无校验、奇校验、偶校验），用于简单的传输错误检测；
4. **停止位**：1 / 1.5 / 2位可选，始终为高电平，标志一帧数据的结束。

#### 3. 关键配置参数
- **波特率（Baud Rate）**：每秒传输的比特数，单位bps。常用值：9600、19200、115200、921600；
- **数据位**：通常设为8位，对应传输一个字节；
- **停止位**：通常设为1位；
- **校验位**：绝大多数场景关闭（无校验）。

#### 4. UART 与 USART 的区别
- **UART**：仅支持异步通信模式；
- **USART（通用同步异步收发器）**：向下兼容UART的全部异步功能，同时额外支持同步通信（可主动输出时钟信号）。

日常开发中，绝大多数场景都把USART配置为普通UART异步模式使用。

### 四、常见误区澄清
1. **UART ≠ RS-232**
UART是通信协议与硬件模块，默认输出TTL电平；RS-232是电气电平标准，底层使用UART的帧协议，需要MAX232等电平转换芯片实现TTL与RS232的互相转换。

2. **串口 ≠ UART**
“串口”是所有串行通信接口的统称，UART只是最常见的一种异步串口；SPI、I²C、CAN等也属于串行接口，但一般不会简称为“串口”。

3. **RS-485不是新的通信协议**
RS-485只是物理层电气标准，数据链路层依然使用UART的异步帧格式；常见的Modbus-RTU协议，就是运行在RS-485物理层之上的应用层协议。

需要我补充串口通信的**常见故障排查方法**吗？